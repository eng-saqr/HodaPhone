<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hodaphone">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HodaPhone">
        <activity android:name=".beggars_listview"></activity>
        <activity android:name=".Beggars"></activity>
        <activity android:name=".Notes">

        </activity>
        <activity android:name=".choice" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".names" />
        <activity android:name=".listview" />
        <activity android:name=".Monthly_Payment_Acivity" />
        <activity android:name=".ShowActivity" />
        <activity android:name=".FirstActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>